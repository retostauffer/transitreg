% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution.R
\name{Transition}
\alias{Transition}
\alias{c.Transition}
\alias{as.matrix.Transition}
\alias{pdf.Transition}
\alias{log_pdf.Transition}
\alias{cdf.Transition}
\alias{quantile.Transition}
\alias{median.Transition}
\alias{mean.Transition}
\alias{random.Transition}
\alias{is_discrete.Transition}
\alias{is_continuous.Transition}
\alias{support.Transition}
\title{Creates a Transition Distribution}
\usage{
Transition(x, bins)

\method{c}{Transition}(...)

\method{as.matrix}{Transition}(x, expand = FALSE, ...)

\method{pdf}{Transition}(d, x, drop = TRUE, elementwise = NULL, ncores = NULL, ...)

\method{log_pdf}{Transition}(d, x, drop = TRUE, elementwise = NULL, ncores = NULL, ...)

\method{cdf}{Transition}(d, x, drop = TRUE, elementwise = NULL, ncores = NULL, ...)

\method{quantile}{Transition}(x, probs, drop = TRUE, elementwise = NULL, ncores = NULL, ...)

\method{median}{Transition}(x, na.rm = NULL, ncores = NULL, ...)

\method{mean}{Transition}(x, ncores = NULL, ...)

\method{random}{Transition}(x, n = 1L, drop = TRUE, ...)

\method{is_discrete}{Transition}(d, ...)

\method{is_continuous}{Transition}(d, ...)

\method{support}{Transition}(d, drop = NULL, ...)
}
\arguments{
\item{x}{Numeric, vector of quantiles.}

\item{bins}{numeric vector of points of intersection of the bins.
The length the vector must be of \code{length(x) + 1} (if \code{x} is
a vector) or \code{ncol(x) + 1} if \code{x} is a matrix. Must be
monotonically increasing.}

\item{\dots}{unused.}

\item{expand}{logical, if FALSE (default) the wide format is
returned, else the extended (long) form.}

\item{d}{An object of class \code{Transition}.}

\item{drop}{Logical. Should the result be simplified to a vector if possible?}

\item{elementwise}{Logical. Should each distribution in \code{x} be evaluated at
all elements of \code{probs} (\code{elementwise = FALSE}, yielding a matrix)? Or, if
\code{x} and \code{probs} have the same length, should the evaluation be done element
by element (\code{elementwise = TRUE} yielding a vector)? The default of \code{NULL}
means that \code{elementwise = TRUE} is used if the lengths match and otherwise
\code{elementwise = FALSE} is used.}

\item{ncores}{Number of cores to be used (see \code{\link[=transitreg]{transitreg()}} for details).}

\item{probs}{numeric vector of probabilities with values in \verb{[0,1]}.
(Values up to ‘2e-14’ outside that range are accepted and
moved to the nearby endpoint.) TODO(R): SURE?}

\item{na.rm}{Unused.}

\item{n}{Integer \verb{>0}, number of random values to be drawn per distribution.}
}
\value{
Returns an object of class \code{c("Transition", "distribution")}.
TODO(R): Missing.

Numeric matrix. If \code{expand = FALSE} the return is of dimension
\code{c(length(x), <ncol>)}.

If \code{expand = TRUE} the returned matrix is of dimension
\code{c(length(x) * <ncol>, 3L)} where the four columns contain
\code{index} (1, ..., length(x)) where each index corresponds to the
row-index of the original input \code{x} (distribution identifier),
\code{theta} which is the 'bin' the transition probability belongs to,
followed by the transition probabilities \code{tp} itself.
This expanded version is used when calling the .C functions.
}
\description{
A 'Transition' distrubiton consists of a series of \code{K} transition
probabilities (TP) for \code{K} 'bins' (counts or pseudo-counts).
}
\details{
TODO(R): Missing
}
\examples{
# TODO(R) Write example

}
\author{
Reto
}
\concept{Transition distribution}
\concept{distributions}
